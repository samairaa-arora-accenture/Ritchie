<Window x:Class="Richie.UI.Views.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Richie"
        Height="300" Width="420"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource ApplicationBackgroundBrush}">
    <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" BorderThickness="1">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <ui:SymbolIcon Symbol="Wallet24" FontSize="48" HorizontalAlignment="Center" />
            <ui:TextBlock FontTypography="TitleLarge" Text="Richie" HorizontalAlignment="Center" Margin="0,12,0,4" />
            <ui:TextBlock Appearance="Secondary" Text="Preparing your secure workspace…" HorizontalAlignment="Center" />
            <ui:ProgressRing IsIndeterminate="True" HorizontalAlignment="Center" Margin="0,20,0,0" />
        </StackPanel>
    </Border>
</Window>

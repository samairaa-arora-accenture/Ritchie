<ui:FluentWindow x:Class="Richie.UI.Views.Assets.SipScheduleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="SIP automation"
        Height="640" Width="480"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="SIP automation" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,8,24,8">
            <StackPanel>
                <ui:TextBlock FontTypography="TitleLarge" Text="{Binding AssetName}" />
                <ui:TextBlock Appearance="Secondary" Margin="0,0,0,16"
                              Text="When a SIP date arrives, the amount is auto-added to this asset and a notification is raised." />

                <CheckBox Content="Enable SIP" IsChecked="{Binding IsEnabled}" Margin="0,0,0,12" />

                <ui:TextBox PlaceholderText="Monthly amount" Margin="0,0,0,10"
                            Text="{Binding AmountText, UpdateSourceTrigger=PropertyChanged}" />

                <ui:TextBlock Text="Investment day of month" Margin="0,0,0,4" />
                <ComboBox ItemsSource="{Binding Days}" SelectedItem="{Binding DayOfMonth}" Margin="0,0,0,10" />

                <ui:TextBlock Text="Frequency" Margin="0,0,0,4" />
                <ComboBox ItemsSource="{Binding Frequencies}" DisplayMemberPath="Text" SelectedValuePath="Value"
                          SelectedValue="{Binding Frequency}" Margin="0,0,0,10" />

                <ui:TextBlock Text="Start date" Margin="0,0,0,4" />
                <DatePicker SelectedDate="{Binding StartDate}" Margin="0,0,0,16" />

                <ui:TextBlock FontTypography="BodyStrong" Text="Upcoming instalments" Margin="0,0,0,4" />
                <ItemsControl ItemsSource="{Binding Upcoming}" Margin="0,0,0,16">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:TextBlock Appearance="Secondary" Text="{Binding}" Margin="0,2" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ui:TextBlock FontTypography="BodyStrong" Text="History" Margin="0,0,0,4" />
                <ItemsControl ItemsSource="{Binding History}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ui:TextBlock Appearance="Secondary" Text="{Binding}" Margin="0,2" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ui:TextBlock Foreground="#C42B1C" TextWrapping="Wrap" Margin="0,8,0,0" Text="{Binding Error}" />
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="24,8,24,16">
            <ui:Button Content="Cancel" Margin="0,0,8,0" Command="{Binding CancelCommand}" />
            <ui:Button Content="Save" Appearance="Primary" Command="{Binding SaveCommand}" />
        </StackPanel>
    </Grid>
</ui:FluentWindow>

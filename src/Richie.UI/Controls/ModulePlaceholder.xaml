<UserControl x:Class="Richie.UI.Controls.ModulePlaceholder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">
    <StackPanel Margin="32" VerticalAlignment="Top">
        <ui:TextBlock FontTypography="TitleLarge"
                      Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        <ui:TextBlock Appearance="Secondary"
                      Margin="0,8,0,0"
                      TextWrapping="Wrap"
                      Text="{Binding Description, RelativeSource={RelativeSource AncestorType=UserControl}}" />
    </StackPanel>
</UserControl>
